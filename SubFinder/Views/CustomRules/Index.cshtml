@model IEnumerable<SubFinder.Models.CustomRule>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Unit)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.ExecutionRule)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.ExecutionParameters)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Unit)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ExecutionRule)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ExecutionParameters)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CustomRuleId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CustomRuleId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CustomRuleId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
